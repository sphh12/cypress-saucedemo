# CLAUDE.md

> Cypress E2E 테스트 자동화 템플릿용 작업 가이드입니다.
> 트리거 섹션 등 일부 항목은 프로젝트에 맞게 커스터마이즈하세요.

## 작업 원칙

- **기존 파일 우선**: 별도 지시가 없으면 기존 파일의 구조·데이터를 기준으로 작업한다. 새 파일 생성이나 구조 변경은 하지 않는다.
- **애매하면 먼저 확인**: 기존 파일을 수정할지, 새로 만들지 판단이 애매한 경우 반드시 사용자에게 먼저 물어본 후 진행한다.

## 파일 수정 안전 규칙 (재발방지)

- **바이트 단위 조작 금지**: `truncate`, `dd`, `head -c` 등 바이트 단위로 파일을 자르는 명령은 절대 사용하지 않는다. 불필요한 문자 제거는 반드시 Edit 도구로 내용을 직접 확인하고 수정한다.
- **수정 후 파일 끝 검증 필수**: 코드 파일 수정 후 반드시 파일의 마지막 5줄을 Read로 확인하여 닫는 괄호(`}`, `});`)가 온전한지 검증한다.
- **수정 후 구문 검증**: JS 파일 수정 후 `node --check <파일경로>` 또는 `npx acorn --ecma2020 <파일경로>`로 구문 오류가 없는지 확인한다.
- **수정 범위 외 영향 확인**: 한 부분을 수정한 뒤, 수정 범위 밖(특히 파일 시작과 끝)이 손상되지 않았는지 반드시 확인한다.

## 트리거

<!-- 아래는 일반화된 기본 트리거입니다. 팀 워크플로(이슈 트래커, 변경 이력 문서, CI 등)에 맞게 자유롭게 커스터마이즈하세요. -->

- "git 확인"/"깃 상태 확인"/"깃 확인해줘" → `git fetch` → 로컬 vs 원격 비교표 제시
- "푸시"/"git push"/"깃에 올려줘" → 민감정보 스캔(시크릿/계정/개인정보) → 변경사항 정리 → 커밋/푸시
- "브리핑"/"현재상황 알려줘"/"진행상황 알려줘" → 현재 브랜치·변경사항·미해결 작업 기반 상황 브리핑

> 참고: 시크릿/계정/개인정보는 절대 커밋하지 않는다. 로그인·테스트 데이터는 `cypress.env.json`(gitignore됨)에서 주입하고, 메일/리포트 자격증명은 환경변수(`SMTP_*`, `MAIL_FROM`, `MAIL_TO` 등)로 관리한다.
